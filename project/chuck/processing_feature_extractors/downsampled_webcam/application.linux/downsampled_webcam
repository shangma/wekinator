#!/bin/sh

APPDIR=$(dirname "$0")
java  -Djava.library.path="$APPDIR" -cp "$APPDIR/lib/downsampled_webcam.jar:$APPDIR/lib/core.jar:$APPDIR/lib/video.jar:$APPDIR/lib/oscP5.jar:$APPDIR/lib/oscP5.jar" downsampled_webcam
